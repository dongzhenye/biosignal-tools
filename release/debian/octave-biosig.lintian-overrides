# as mentioned in README.source, tinyxml did not accept the patch (yet)
# so libbiosig comes with its own patched (supports compression) copy
libbiosig16 binary: embedded-library usr/lib/libbiosig.so.*: tinyxml
